# 通用需求规格说明书规范 - 使用指南

本文件配合《通用需求规格规范.md》使用,说明如何在实际工作中落地该规范:从拿到 PM 的需求材料和 UI 文件,到最终产出符合规范的需求文档。三份规则文件（核心规则 / 示例 / 附录）均不强制引用本指南,按需查阅。

---

## 整体流程

```
PM 需求文档(docx/pdf) ─┐
                        ├─→ ① 拆功能 → ② 套模板生成 → ③ UI 同步 → ④ 模型草稿与确认 → ⑤ 自检 → ⑥ 评审 → ⑦ 迭代
UI 设计文件(Figma/图/HTML)┘
```

---

## ① 输入准备

- **需求文字**:PM 提供的 docx/pdf,作为业务逻辑、角色、规则的来源。
- **UI 设计**:Figma 链接、Axure/HTML 原型、截图等。不同格式的处理方式：

| UI 文件类型 | 处理方式 |
|---|---|
| Figma 设计稿 | 读取页面/区域结构,整理为原型引用；如需可点击 HTML 原型,需另行生成放入 `ui-design/` |
| Axure / 已导出的 HTML 原型 | 直接复制到 `ui-design/{功能名称}.html`,按规则加链接 |
| Sketch / XD 设计文件 | 先导出为图片或 HTML,再按图片/HTML 两种方式处理 |
| 截图 / PDF 里的设计图 | 标「原型：待生成」,截图作为参考图单独附上并注明"仅供参考/非最终方案"；或包一层简单 HTML 放入 `ui-design/` 以满足链接引用格式 |
| 暂无 UI 文件 | 界面原型章节写「不适用」或「待生成」,不能空着 |

- **数据模型**:分两种场景——模型已存在（如中台/其他系统提供）直接引用独立文档；模型待设计（本项目需根据需求设计数据库）则走 ④ 步骤起草+确认。两种情况下需求规格正文都不直接定义数据模型（见核心规则 §4）。

---

## ② 按规则生成需求文档

对照《通用需求规格规范.md》§0 使用步骤逐条执行:

1. 划清**功能边界**(一个相对完整的业务能力 = 一章),依次编号 `## 1. {功能名称}`、`## 2. {功能名称}` ……
2. 每章「功能概述」标注**功能类型**(列表查询 / 表单录入 / 详情只读 / 流程审批 / 只读展示或报表 / 批量导入导出 / 后台任务或接口能力 / 其他),据此启用 §3.3 对应的附加要求。
3. 按模板填写:
   - N.1 功能概述(功能类型、角色、用途、范围边界)
   - N.2 界面原型（或「不适用」+ 原因）与界面交互
   - N.3 业务逻辑(拆分为 N.3.1、N.3.2……,每条对应一个页面元素/操作点/处理步骤)
   - N.4 验收要点(可选,建议默认生成简版)
   - N.5 遗留问题(可选,存在待定内容时使用)
4. 涉及数据的条目只写「引用数据模型」：模型已存在写文档文件名 + 模型章节 + 模型名称；模型待设计写业务实体名称并标「模型待设计」（后续走 ④ 步骤）。均不在正文定义字段全集、约束、关系图。
5. 不确定的内容标 `TODO`/「待定：…」,不当作确定需求写入；仅用于演示的内容标「示例 / 仅供参考 / 非最终方案」。

多个功能可以在同一份文档内按章节顺序累加,也可以按模块拆成多份文档,每份内部仍以功能为章节单位。

---

## ③ UI 文件同步进文档

- **目录约定**:功能级原型放 `./ui-design/{功能名称}.html`；跨功能复用元素放 `./ui-design/_shared/`；单功能专属元素（弹窗/抽屉等）放 `ui-design/` 根目录并以「{功能名称}-」为前缀。
- **引用方式**:必须用 Markdown 链接引用,禁止手写 `<a href>`：
  ```markdown
  [{功能名称}原型](./ui-design/{功能名称}.html)
  ```
- 原型未就绪时,链接位置标注「待生成」,不能空着不提；也不能用文字描述原型中不存在的视觉细节（字体/颜色/间距等）来代替原型文件。
- 界面交互（`#### 界面交互`）：原型已能直接体现的交互（点击即跳转/弹窗）可省略描述；原型仅静态展示时必须用表格描述触发元素、触发方式、交互类型、目标、传递上下文、说明。

---

## ④ 模型草稿与确认（涉及数据、且模型待设计时）

- **模型已存在**（中台/其他系统提供）：直接引用其独立模型文档，跳过本步骤。
- **模型待设计**（本项目需根据需求设计数据库）：
  1. 需求定稿后，从业务逻辑条目中提取涉及的业务实体、字段（中文+英文）、数据处理要求（过滤/聚合/关联/持久化）。
  2. 由 AI 基于以上信息输出**模型草稿**（实体、字段、类型建议、关系、必要索引/约束建议），状态标注「草稿」（参考附录 F）。
  3. 开发/DBA **人工确认**（补充索引、审计字段、脱敏、命名规范对齐、与现有表复用关系等技术细节），确认后才形成正式《{项目}-需求规格-业务数据模型》文档。
  4. 需求文档中「引用数据模型」补齐正式引用路径（文档文件名 + 模型章节 + 模型名称）。
- **禁止**：模型草稿未经人工确认直接执行为 DDL 或投入开发。

---

## ⑤ 提交前自检

每次生成或修改完成后,过一遍《通用需求规格规范.md》§0 提交前自检清单:

- [ ] 文档头含版本、适用范围；变更日志已更新（若有修改）
- [ ] 术语/缩略语已定义（若使用）
- [ ] 每章概述含功能类型、角色、用途、范围边界
- [ ] 确定 / 待定 / 示例已区分,无「待定当确定」
- [ ] 有 UI：有原型 Markdown 链接或「待生成」；无 UI：已写不适用及原因
- [ ] 交互在需要时已描述,且目标与业务逻辑名称一致
- [ ] 业务逻辑条目含功能要求；涉及数据则有模型引用与处理要求
- [ ] 权限与可见性、异常与空态已写或标「不适用」
- [ ] 建议具备可验证的验收要点
- [ ] 正文无数据模型定义全集、无审核结论、无 `<a href>`

同时对照核心规则 §6 禁止项汇总,逐条排除。

---

## ⑥ 评审

- 评审意见/结论**不写入需求正文**,外置到 `{需求文档目录}/review/`。
- 遗留问题（N.5）需记录：待定事项、影响范围、候选方案（如有）、需谁确认；结论确定后转为正式描述并删除该行,不能一直挂着。

---

## ⑦ 迭代与变更

| 变化 | 处理方式 |
|---|---|
| 需求内容变化 | 修改对应章节,并在文档头「变更日志」追加一行（时间、变更人、变更内容需引用章节名称）；禁止伪造历史时间/责任人 |
| UI 设计变化 | 直接替换 `ui-design/` 下同名 HTML 文件；若文件名不变,需求文档里的链接不用改 |
| 数据模型变化 | 修改独立的模型文档；若模型名称/章节号变化,同步更新需求正文中的引用 |
| 影响范围较大的变更 | 可参考附录 D「变更影响」补充：影响的功能章节、影响的原型文件、影响的模型文档章节 |

---

## 涉及的外部文档一览

| 内容 | 存放位置 |
|---|---|
| 需求规格正文 | 项目约定的需求文档目录 |
| 界面原型 HTML | `./ui-design/`（与需求文档同目录） |
| 业务数据模型 | 独立模型文档,如《{项目}-需求规格-业务数据模型》 |
| API/DDL 设计 | 独立设计/模型文档 |
| 评审记录 | `{需求文档目录}/review/` |

---

## 参考

- 核心规则:《通用需求规格规范.md》
- 完整示例（CRUD、只读展示/报表）:《通用需求规格规范-示例.md》
- 按场景模板（字段表、状态流转、导入导出清单、变更影响、业务实体草稿、反例）:《通用需求规格规范-附录.md》
