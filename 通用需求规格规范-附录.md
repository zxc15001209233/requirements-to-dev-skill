# 通用需求规格说明书规范 - 附录

本文件配合《通用需求规格规范.md》使用，以下模板在对应场景**推荐使用**，非每章强制。

---

## 附录 A：字段表（列表 / 表单）

| 字段（中文） | 英文名 | 控件/展示 | 必填 | 校验/说明 |
|---|---|---|---|---|
| {中文} | {english} | 输入框/下拉/只读… | 是/否 | {规则} |

---

## 附录 B：状态与流转（流程类）

**状态列表**：{状态枚举及含义}

| 从状态 | 到状态 | 动作 | 条件 | 操作角色 |
|---|---|---|---|---|
| {from} | {to} | {动作名} | {条件} | {角色} |

---

## 附录 C：导入导出检查清单

- [ ] 导入模板字段与说明
- [ ] 校验规则与失败行处理（整单失败 / 部分成功）
- [ ] 导入权限与操作审计（若需要）
- [ ] 导出范围（当前页 / 当前筛选 / 全部，须明确）
- [ ] 导出权限与敏感字段脱敏（若需要）

---

## 附录 D：变更影响（功能变更时可选）

变更日志之外，可补充：

- 影响的功能章节
- 影响的原型文件
- 影响的模型文档章节

（仍不在正文写审核结论。）

---

## 附录 E：反例（避免）

| 错误写法 | 问题 | 正确方向 |
|---|---|---|
| 「系统应友好易用，性能要好」 | 不可验证、无边界 | 写成可测验收点或具体约束 |
| 在需求中贴整张字段 DDL | 模型定义进错文档 | 引用《业务数据模型》章节 |
| 「见相关文档」 | 无法定位 | `《文档名》x.x 名称` |
| 「与现网一致即可」且无说明 | 待定被伪装成确定 | 标 TODO，或写明对照对象与要点 |
| 正文写「评审通过，无问题」 | 审核结论进正文 | 外置到 `review/` |
| AI 起草的模型草稿未经确认直接建表/写 DDL | 草稿当已确认，跳过人工把关 | 须经开发/DBA 确认后才转正式模型文档（见附录 F） |

---

## 附录 F：业务实体草稿（模型待设计时，AI 起草 + 人工确认用）

**填写方式**：一行对应**一个字段**；同一实体的多个字段重复填写「实体」列（保持实体名称一致）；「关系」列填该字段的关联关系（如外键关联的实体），无则写「无」。

| 实体（中文/英文） | 字段（中文/英文） | 类型建议 | 关系 | 状态 |
|---|---|---|---|---|
| {中文}（{English}） | {中文}（{english}） | {如 varchar/int/datetime} | {关联实体/说明，无则写"无"} | 草稿 / 已确认 |

**使用说明**：AI 根据需求业务逻辑条目中的实体、字段、数据处理要求提取生成本表；开发/DBA 确认无误后，将「状态」改为「已确认」，并产出正式《{项目}-需求规格-业务数据模型》文档，需求正文中的引用同步更新为正式路径。确认前，本表内容**禁止**直接执行为 DDL 或用于开发。
