# 通用开发计划管理规范

本文件为**核心规则**。配合《通用需求规格规范.md》与《通用技术选型规范.md》使用——前者定义"做什么"，后者定义"用什么技术"，本规范定义"怎么拆、怎么排、怎么跟"。完整示例见《通用开发计划管理规范-示例.md》；任务包组装模板见《通用开发计划管理规范-附录.md》；完整使用流程见《通用开发计划管理规范-使用指南.md》（均与本文件同目录）。

---

## 0. 使用步骤与提交前自检

**使用步骤**：

1. 按本 Skill《SKILL.md》判定轻量路径 / 完整路径，并已走过对话确认卡。完整路径必须使用本规范；轻量路径默认不写独立开发计划（实施顺序写在需求文档中），仅当用户点名「拆开发计划」时才按本规范编写。
2. 确认需求规格文档已完成（走完其 §0 自检、评审，遗留问题已清零或已知悉可接受）。完整路径还须已具备设计契约（数据模型已确认、接口契约、横切约定，见本 Skill《SKILL.md》）。
3. 按本规范 §2 以需求文档的功能章节为单位拆分开发模块；完整路径须另设基座/横切与联调模块（若适用）。
4. 每个模块按 §3 模板填写，标注依赖关系与验收对应。
5. 技术栈直接引用《通用技术选型规范》，不在本文档重新定义。
6. 执行时按 §4 状态流转更新模块状态；需求变更时按 §5 同步开发计划。
7. 提交前用下方清单自检。

**提交前自检清单**：

- [ ] 每个模块均可定位到需求文档的具体章节号（不能写「见需求文档」了事）
- [ ] 每个模块已填写「完成口径」；标「已完成」时已通过该口径，未用「真接口未测」卡住契约落盘模块
- [ ] 模块间依赖关系已理清，排序符合依赖顺序（如：数据模型确认 → 接口 → 前端）；下游只要求前置模块的完成口径已达成
- [ ] 每个模块的验收对应已引用需求文档的验收要点（N.4），或说明「不适用」
- [ ] 技术栈已引用《通用技术选型规范》，无重复定义；§2 数据库等实际值已填（或「本项目不用」/沿用仓库）
- [ ] 无排期/人力分配的详细内容（如需要，外置到项目管理工具）
- [ ] 状态字段与实际进度一致，无长期停留「进行中」却无更新的模块
- [ ] 已按本 Skill《SKILL.md》判定路径；完整路径已引用设计契约，且含基座/联调模块（若适用）

---

## 1. 适用范围

- **完整路径强制**使用本规范（判定见本 Skill《SKILL.md》「规模分流」）。
- **轻量路径默认不写**独立开发计划；用户点名时才写，模块模板与状态字段与本规范一致。
- 本规范适用于：已确认（完成自检与评审）的通用需求规格文档转化为开发任务的场景。
- 本规范**不适用于**：需求本身的编写（见《通用需求规格规范》）、技术选型的确定（见《通用技术选型规范》）、设计契约正文（模型/接口/横切，见本 Skill《SKILL.md》）、项目排期与人力分配（见项目管理工具/项目计划）。
- 输入：一份或多份已定稿的需求规格文档（完整路径另含设计契约）；输出：一份开发任务清单（本规范定义的模块列表）。

---

## 2. 文档结构

### 2.1 组织单位

- **以需求文档的功能章节为组织单位**：需求文档的 `## N. {功能名称}` 对应开发计划中的一个或多个模块，编号建议为 `M{N}`、`M{N}.1` ……，与需求章节号保持可追溯。
- 一个功能章节内业务逻辑条目（N.3.x）较多、涉及不同技术分层时，可拆成多个模块（如：数据模型确认、查询接口、列表页、编辑弹窗）；简单功能可合并为一个模块。
- 完整路径除功能章节外，应增加**基座/横切**（登录、权限、全局时间、组织范围、环境配置等）与**联调/验收**模块，避免只按功能章拆完却无法上线。

### 2.2 文档级元信息（必选，宜短）

```markdown
# {开发计划标题}

## 计划说明
- **对应需求文档**：《{需求文档名称}》{版本}
- **技术栈**：见《通用技术选型规范》§2 本项目实际值（如有覆盖需注明）
- **生成/更新时间**：{时间}

## 变更日志
| 时间 | 变更人 | 变更内容（须引用模块编号） |
|---|---|---|
```

---

## 3. 模块条目模板

```markdown
### M{N}.{序号} {模块名称}

- **引用需求章节**：《{需求文档名称}》{章节号} {功能/条目名称}
- **技术分层**：数据模型 / 接口 / 前端页面 / 前端组件 / 后台任务 / 其他
- **依赖模块**：{前置模块编号}，无则写「无」
- **验收对应**：《{需求文档名称}》{章节号} 验收要点第 {n} 条，无则写「不适用」
- **完成口径**：契约落盘 / 页面或 mock 自测 / 真接口联调
- **状态**：待开始 / 进行中 / 已完成 / 阻塞
- **阻塞说明**（状态为阻塞时必填）：{阻塞原因、需谁解决}
```

**要求**：

- 「引用需求章节」必须可定位到具体章节号，禁止写「见需求文档」。
- 数据模型相关模块，若需求中标注「模型待设计」，本模块需引用《通用需求规格规范》§4 与附录 F 的起草+确认流程，模块验收对应写「模型文档人工确认通过」。
- 「技术分层」涉及的具体技术细节（用什么组件、什么协议）引用《通用技术选型规范》，不在本处展开设计。
- **完成口径**按分层默认：数据模型 → 契约落盘；接口 → 真接口联调（若本模块只写契约，必须改写「契约落盘」）；前端页面/组件 → 页面或 mock 自测；联调模块 → 真接口联调。
- 「已完成」= 已通过**本模块所写完成口径**，不是三条口径都过。禁止用「没接 Spring / 真接口未测」阻止完成口径为「契约落盘」的模块标已完成。
- 下游依赖的是前置模块状态为「已完成」（其口径已达成）。接口模块口径为契约落盘且已完成时，前端可以开工；联调模块完成仍要求真接口联调。

---

## 4. 状态流转与执行

**状态定义**：

| 状态 | 说明 |
|---|---|
| 待开始 | 依赖未满足或尚未安排 |
| 进行中 | 已开始开发/设计，未完成**本模块完成口径** |
| 已完成 | 已通过本模块完成口径对应的自测 |
| 阻塞 | 因依赖、需求不明确或技术问题无法推进，需说明原因 |

**执行建议**：

- 按依赖顺序执行：数据模型确认 → 接口/契约 → 前端页面/组件 → 联调 → 对照验收要点自测。
- 使用 AI 辅助开发时，按《通用开发计划管理规范-附录》组装任务包（需求章节原文、验收要点原文、设计契约相关节、技术栈、模型、**完成口径**）；**禁止只把模块名称丢给 AI**。一次 `go` 覆盖确认卡上的模块范围，范围内连续执行，不要每模块再要一次空 `go`。
- 模块完成后，必须对照「完成口径」实际自测，通过才标记「已完成」，不能仅凭代码写完即标记完成。完成口径不是「真接口」时，不要拿真接口未测当未完成理由。

---

## 5. 变更联动

- 需求文档发生变更（其 §2.2 变更日志有新记录）时，需检查是否影响已拆分的模块：
  - 影响范围内的模块状态回退为「待开始」或「阻塞」，并在本文档变更日志中记录。
  - 新增功能章节需补充对应模块。
- 技术栈发生变更（覆盖《通用技术选型规范》默认值）时，本文档「技术栈」字段同步更新，不需要逐模块重复说明。

---

## 6. 禁止项汇总

- ❌ 模块「引用需求章节」写「见需求文档」而不给具体章节号。
- ❌ 在开发计划中重新定义技术选型细节（应引用《通用技术选型规范》）。
- ❌ 在开发计划中写入详细排期（起止日期）、人力分配（属于项目计划/项目管理工具）。
- ❌ 模块状态与实际进度不一致（如已完成却未过该模块完成口径自测）。
- ❌ 用「没接 Spring / 真接口未测」阻止完成口径为「契约落盘」的模块标已完成。
- ❌ 确认卡已写 Mx–My 仍对范围内每一模块再要一次无内容的 `go`。
- ❌ 需求变更后不同步更新受影响模块的状态。
- ❌ 模块拆分脱离需求文档的功能章节，另起一套不可追溯的分类。
- ❌ 完整路径在无开发计划、或依赖模块未达其完成口径、或未组装任务包时写业务代码。
- ❌ 完整路径只拆功能章、不设基座/横切或联调模块（若项目存在跨功能约束或分期集成）。

---

## 7. 与其他文档的边界

| 内容 | 是否放在本规范 |
|---|---|
| 模块拆分、依赖、状态跟踪 | ✅ |
| 需求文档的功能行为、验收要点 | ❌ 《通用需求规格规范》 |
| 技术栈选型 | ❌ 《通用技术选型规范》 |
| 详细排期、人力分配 | ❌ 项目计划/项目管理工具 |
| 数据模型定义 | ❌ 独立模型文档 |
| 接口契约、横切约定 | ❌ 设计契约（见本 Skill《SKILL.md》） |
| 规模分流、就绪门禁 | ❌ 本 Skill《SKILL.md》 |

---

## 8. 参考资料

- 路径判定与就绪门禁：本 Skill《SKILL.md》
- 需求来源：《通用需求规格规范.md》
- 技术栈来源：《通用技术选型规范.md》
- **完整示例**（模型待设计 / 模型已存在两种场景对比）：见《通用开发计划管理规范-示例.md》
- **任务包组装清单与模板**（执行单个模块时如何打包给大模型）：见《通用开发计划管理规范-附录.md》
- **完整使用流程**：见《通用开发计划管理规范-使用指南.md》

三份均与本文件同目录，非强制引用，按需查阅。
