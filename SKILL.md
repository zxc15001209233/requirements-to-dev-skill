---
name: requirements-to-dev
description: Use when writing, reviewing, or revising 需求规格, 技术选型, 开发计划, or 设计契约; when a document just landed and needs independent review before the next; when starting a new business project from PM materials or UI; when the user says 根据提供的材料开始开发; when the user mentions 功能章, 轻量路径, 完整路径, or 任务包; when 需求规格/选型/开发计划 must not be produced together; when unconfirmed questions must be asked before producing a document; when implementation starts before 开发计划 or 设计契约.
---

# 需求到研发

用户只需提供材料；本 Skill 转成需求规格，再按模块做到可验收。用户不必自己写规格模板。不覆盖编码风格、排期人力、部署运维、CI/Jenkins、本机 JDK 路径。

就绪门禁、模块状态以**本 Skill**为准。协作门禁（`go` / 变更日志 / 审核外置 / bug 目录）：**项目已有总规则则用项目的**，本 Skill 不覆盖；项目没有总规则时，使用下一节默认门禁。不要求项目存在特定文件名或 Cursor `.mdc`。

**如何判定「已有总规则」**：项目规则目录里已有总规则正文（如 `AI工作规范.md` / 同名 `.mdc`，或该编辑器 always-apply 的等价规则）。不要凭 README 一句「请遵守 go」当成总规则。

细则按阶段再读**本目录**附件，不要把全部规范一次性塞进上下文。附件必须与 `SKILL.md` 放在同一目录，不要只拷主文件。

用户说「根据提供的材料开始开发 / 按这份文档开始 / 转换并开发」时，这是**讨论**：读材料、问歧义、出**需求规格**确认卡。不算 `go`，不得直接写代码，不得把选型、模型、契约、计划写进同一张卡。

界面原型若是数据可视化大屏 / 看板 / 驾驶舱：委托 `bi-dashboard-generator` 出确认单与 **HTML 原型**；本 Skill 只把原型路径写入需求章节，不在本 Skill 内画大屏或写图表 option。默认栈是 Vue 3 + Element Plus 与 Java + Spring Boot，**按选型 §2 写业务代码**必须交回本 Skill，且须已有已审核开发计划。对方未安装则停，请用户安装或点名调用，禁止空口假装已按对方流程做完。

用户只说「按这份文档开始」、未点明只要大屏时，由本 Skill 先判定范围。文档同时含大屏页与中后台/CRUD 页：大屏页委托 `bi-dashboard-generator`（仅 HTML），其余章节由本 Skill 写；禁止两边各做一整份。纯大屏与混合中的大屏页：**需求规格人审并「下一步」之后，下一张卡是委托 HTML**，不得先出选型卡。

## 协作门禁（仅当项目没有总规则时使用）

**没有 `go`，禁止任何文件的创建、修改、删除。**

- 一次 `go` = 确认卡上写明的范围（一份方案、一组文件、或从 Mx 到 My 的模块范围）。禁止把已确认范围内的模块拆成无内容的连点 `go`。
- 用户说「好的 / 同意 / 可以」不算授权。
- 将改文件的方案讨论须：提出理解 → 列出问题 → 提出方案 → 等 `go`。纯问答（解释代码、查日志、问术语）直接答，仍不改文件。
- 读文件、搜索、分析、展示草稿：无需 `go`。
- 若环境支持子 Agent / 委派任务：主对话里已 `go` 确认的改文件任务，委派执行时免再次 `go`；不得扩大范围。无委派能力的环境忽略本条。
- 即使收到 `go`：有误解、缺关键信息、或未过就绪门禁时，停下再确认。

修改需求/设计文档后必须追加变更日志（时间 `YYYY-MM-DD HH:MM`、变更人、变更内容须引用章节名）。变更人未指定时写「用户确认（对话）」；禁止编造人名或伪造时间。

审核结论不写进正式文档正文，外置到被审文档所在目录的 `review/`（`X.md` → `review/X-审核-YYYYMMDD.md`）。落盘后的独立机审见「独立机审」节；**禁止**只说「请手动审核」而不出 `review/` 文件。

仅当用户明确说 bug / 缺陷 / 故障或要求记单时：先对齐描述；记录在 `./bugs/bug-YYYYMMDD-简述/`；解决前先写 `bug-fix-solution.md`。未点名 bug 的观感问题不强制建档。

## 统一流程（强制；不再分轻量 / 完整）

禁止再按功能章数量、是否要后端联调等条件走捷径。用户提到「轻量路径 / 完整路径」时，仍用本 Skill，但**一律按下述顺序**，不得恢复分流。

确认卡第 1 项写「统一流程」+ 复述**本项目对应顺序**，禁止填「轻量」或「完整」，禁止用计数表判定。

**永远同一条（一份一审，手动确认下一步）。确认卡第 1 项按项目类型复述对应顺序：**

非大屏：

```
需求规格
  → 技术选型 §2
  → 已确认数据模型
  → 设计契约
  → 开发计划
  → 实现（按 §2 实际栈写业务代码；确认卡写 Mx–My）
```

纯大屏，或混合材料里的大屏页：

```
需求规格
  → HTML 原型（委托 bi-dashboard-generator；不得与规格同一轮 go）
  → 技术选型 §2
  → 已确认数据模型
  → 设计契约
  → 开发计划
  → 实现（按 §2 实际栈；确认卡写 Mx–My）
```

HTML：大屏路径上，规格人审并「下一步」后**必须先委托 HTML**（走对方确认单与 Step 5.5），人确认原型后再出选型卡。非大屏的界面原型：文档阶段可引用已有或标「待生成」，仍不得与需求规格同一轮 `go`。需求里的「实施顺序」不能代替开发计划。

**最终最低产物**（分阶段写出，不是第一次 `go` 的清单）：需求规格 +（大屏则已审 HTML）+ 选型 §2 + 已确认数据模型 + 设计契约 + 开发计划。

开发计划须含基座/横切与联调模块，不能只拆功能章。技术栈只引用 [通用技术选型规范.md](通用技术选型规范.md) §2。Vue / Spring 只是默认值，以 §2 为准。§2 任一行仍是「按项目补充」或空白（尤其是数据库）→ 选型未定，不得写业务代码。仓库已有可运行工程时，§2 填该仓库现用栈。不要在需求或开发计划里争论选型。

**无库 / 只用 mock（压薄，不删环节）**

§2 已写「本项目不用」数据库或「只用 mock」后，仍须各出一份模型、契约、计划（仍一份一审），但正文按下面收薄。禁止用本条跳过文件或恢复「轻量路径」。

- **数据模型**：业务实体、字段名、口径；禁止当 DDL、禁止写建表语句或假装有中台表。
- **设计契约**：写成对接约定——字段名、筛选、错误语义、`USE_MOCK`；路径可写「本项目无独立 HTTP 资源」或占位。不是路径 / 错误码 / DDL 全文（那也不是大屏 Skill 的占位所能代替）。
- **开发计划**：仍含基座/横切与联调；完成口径可以是「页面或 mock 自测」，禁止因「没接真库」卡住。

## 文档串行与先问后写（强制）

用户原话（逐字遵守，不得改写放宽）：

- 这三份文档不能一起出，没生成一个文档需要审核，然后手动确认下一步。
- 生成文档过程中需要问题需要确认，不确认的问题，需要询问，不能一下产出文档。

「这三份」= **需求规格**、**技术选型（§2）**、**开发计划**。**数据模型**、**设计契约**同样：每次恰好一份。

**一份一出**

- 一次 `go` 只落盘**一份**文档（一个文件）。确认卡第 4 项只能出现这一份的路径。
- 一份落盘后**立刻独立机审**（见「独立机审」），再把结论交给用户。在用户**手动确认下一步**之前，禁止出下一份确认卡，禁止写下一份。机审通过不等于已授权下一步。
- 默认顺序见上一节。大屏：规格已审、HTML 未审时，用户说「下一步」→ **委托 HTML**，禁止出选型卡。选型已审、计划未出时，用户说「下一步」→ 下一份是**数据模型**（没有则设计契约），**禁止**出实现确认卡。
- 「最终最低产物」不是第一次 `go` 的清单。

**先问后写**

- 写某一份之前：该份会写成**确定**内容的问题必须先问、等答。未问或未答的不得写成确定，也不得写进该份正文当默认算法/范围/列集。
- 歧义清单只服务**即将写的那一份**，不要把选型问题掺进需求规格清单，也不要一次问完全部文档的题然后连写多份。
- 用户答「待定 / 按建议 / 选某方案」之后，才能把该项放进该份确认卡或 N.5。

**禁止（基线失败，不准再用）**

- 同一轮 `go` 写出需求规格、选型、数据模型、设计契约、开发计划中的两份及以上。
- 用确认卡上的「建议默认 + `go` 接受建议」代替逐项询问。
- 「材料短 / 不挡拆章 / 可进 N.5 / 用户要开发计划 / 第一次 go 只写文档 / 轻量可以不写计划 / 实施顺序能代替计划 / 无库就可以不写模型契约」为理由一次成文、跳过模型/契约/计划、或多份一起出。
- 把未问的图/字冲突、公式、主键、范围边界写成确定需求。
- 无已审核开发计划仍出实现确认卡，或把「下一步」默认成按默认栈写工程。
- 大屏规格已审却跳过 HTML、直接出选型或实现确认卡。

实现阶段仍按模块范围 `Mx–My` 一次 `go` 连续做，不把「一份文档」套到每个源码文件上。

## 独立机审（强制；不能代替人审）

写稿 Agent 的「请审核」只挡住连写，挡不住自己放过自己。落盘后必须另开**独立机审**，再等人审授权下一步。

**两层不得合成一层**

| 层 | 谁做 | 作用 | 能否代替「下一步」 |
|---|---|---|---|
| 机审 | 独立审核角色（不带写稿对话） | 对照确认卡 + 规范 + 已落盘文件找问题，写入 `review/` | 不能 |
| 人审 | 用户 | 接受/驳回结论，授权下一份或实现范围 | 这才是门禁 |

**何时审**

- **必审**：需求规格、选型 §2、数据模型、设计契约、开发计划，每一份落盘后立刻审。
- **HTML 大屏原型**：走 `bi-dashboard-generator` Step 5.5（ref/proto 并排），不要再用本 Skill 出一份「文档审」凑数。
- **业务代码**：按确认卡上的 **Mx–My 整段**做完再审这一段，禁止每个源码文件审一次。默认栈常见为 Vue / Spring，以 §2 为准。
- 写稿 Agent 自己再勾一遍自检清单，**不算**独立机审。

**怎么审**

- 审核角色只拿：被审文件、该份确认卡要点、对应规范（规格/选型/模型附录 F/契约三件套/计划模板）。**禁止**把写稿会话历史交给它当「已经对过」。
- 环境支持子 Agent 则委派（主对话已 `go` 写该份时，机审委派免再 `go`；不得扩大去改正文）。
- 无子 Agent：主对话另开一段只读审，必须重新打开文件对照，禁止凭写稿记忆宣布通过。
- 结论只写 `review/{被审文件名}-审核-YYYYMMDD.md`（设计契约在 `documents/design/review/`）。禁止写入需求/选型/模型/契约/计划正文。最低结构：对照范围（确认卡 + 规范）、P0、P1、结论（有 P0 不得声称可进下一份；无 P0 仍须人审下一步）。禁止只写「已同构 / 无问题」。
- 机审发现的问题：**不经用户对修复范围发 `go`，不得改正文**。P0 须在对话里点名；用户仍可「下一步」表示接受已知风险。
- 机审通过或仅有 P2：**仍须等人审「下一步」**，禁止因「审过了」直接出下一份确认卡。

**机审找什么（P0 未清不得声称该份可进入下一份）**

- 确认卡 1–4 项或已确认口径未兑现到正文
- 确定 / 待定 / 示例混写；未问的图字冲突写成确定
- 功能章漏「不做」；计划缺基座/联调；选型数据库仍「按项目补充」
- 契约接口对不上规格章节；模型当 DDL 或字段全集塞进需求正文
- 大屏规格已审、HTML 未审却声称可进选型；无库项目把模型/契约写成 DDL 全文

**禁止**

- 只提醒「请手动审核」而不出 `review/` 文件
- 用机审通过跳过用户「下一步」
- 机审顺手改文件（那是扩大 `go`）
- 每个 `.vue` / `.java` 都开一次审

## 材料澄清（该份确认卡之前）

读完 PM 材料后，针对**即将写的那一份文档**列歧义清单，**等用户回答后再出该份确认卡**。禁止未列清单就出确认卡，禁止把未答的歧义写成确定需求，禁止跳过询问直接成文。

歧义清单每条须含：问题、材料里的依据（章节/原句）、不问清会怎样（拆错章 / 选错栈 / 做成确定需求）。

**必须先问（本份未答完：不得出该份确认卡、不得写该份）**

挡拆章/选型（最低集，有则必问）：

- 功能边界对不上，无法列出功能章
- 大屏页与中后台页混在一起，范围未判定
- 角色、主成功场景互相矛盾
- 默认技术栈与材料明显冲突（仅当本份是选型时）
- 扫描件/只有图没有字，读不出正文

以及：凡写入**本份**正文将变成确定需求，且材料有多种读法、图与字冲突、或材料没写的项——包括范围（做/不做、跳转 vs 本页筛选）、计算口径（公式、分类、环比对象、主键）、名单听表还是听图。不问清就会偷选。

**问过之后用户明确答「待定」的**：进该份 N.5 / 确认卡待定，不算未问。

**不必问（不要用本条跳过上一类）**

- 材料只写了一种做法且无冲突：按已写的做
- 已划出的不做项
- 仅联调才核对的源码/取值映射（开发先按材料原样）
- 材料已写「待定 / 视情况 / 以后再说」且用户已知悉
- 纯文案润色、未写的以后再说增强

禁止把「不必问」理解成「不挡拆章就可以不出清单、直接出确认卡」。

## 阶段确认（写任何落地文件前必做）

先在**对话里**出确认卡，**禁止先落文件**。须已做完上一节材料澄清（本份必须先问的项用户已答，或已明确答待定）。用户对确认卡发 `go` 后，只写卡上那**一份**文档，或实现时卡上的 Mx–My。

确认卡必须包含：

1. **流程**：统一流程（复述本项目顺序：非大屏为规格 → 选型 §2 → 数据模型 → 设计契约 → 开发计划 → 实现；大屏为规格 → HTML → 选型 §2 → 数据模型 → 设计契约 → 开发计划 → 实现）。禁止写轻量/完整或计数依据。
2. **功能章清单**：每章名称 + 一句范围（含明确不做）。需求规格确认卡必填；后续文档引用已审核规格章节，不重新发明清单。
3. **技术选型**：需求规格确认卡可只写「选型本份不写，下一步单独出卡」。大屏规格卡写明下一步委托 HTML。**选型确认卡**必须写出前端栈、后端栈、**数据库实际值**（或「沿用本仓库已有：…」；不用库则写「本项目不用」）。§2 仍为「按项目补充」或空白时不得声称选型已定、不得写业务代码。
4. **本次 `go` 只写哪一份**：文档阶段填**恰好一个**文件（需求规格 / 选型 §2 / 数据模型 / 设计契约 / 开发计划 五选一，写路径），**或**大屏「委托 HTML 原型」（规格已审；本 Skill 不画图，只委托）。不可与实现同时写。写实现时不填文档名，改为 **模块范围**（Mx 到 My）和各段 **完成口径**。建议口令：`go 写需求规格`；`go 委托大屏 HTML`；`go 写选型 §2`；`go 写数据模型`；`go 写设计契约`；`go 写开发计划`；`go 执行 M1.2–M1.4（页面或 mock 自测）`。

禁止：

- 未出确认卡，或卡上 1–4 未对齐，就写规格 / 选型 / 模型 / 契约 / 计划 / 代码。
- **同一轮 `go` 写两份及以上文档，或既写任一文档又写业务代码。** HTML 大屏原型仅在已委托且确认单范围内，仍不得与需求规格同一轮。
- 把聊天里的口头方案当成「需求章节已确认」。
- 用户说「根据提供的材料开始开发 / 转换并开发 / 顺便做出来 / 梳理需求制定计划并开发」时，仍先出**需求规格**确认卡；不得把选型、模型、契约、计划写进同一张卡，不得直接写业务代码。

例外：用户明确说「按已有的《…需求规格》开发」，且该章节文件已存在且用户已审核 → **不得**直接写实现。大屏缺已审 HTML 先出 HTML 委托卡；缺选型先出选型卡；缺模型/契约/计划先出对应文档卡。四份都已审核（大屏还须 HTML 已审）后，才能出实现确认卡。

**文档阶段（每次 `go` 一份；上一份须已独立机审落盘，且用户已确认下一步）**

最终要有（仍须一份一份写）：需求规格；（大屏）已审 HTML；选型 §2；已确认数据模型；设计契约；开发计划。界面原型：大屏按上一节顺序委托；非大屏已有则引用，尚未生成则标「待生成」。本阶段不写业务代码。

**实现阶段（仅当开发计划已审核，且用户点名实现或对实现卡发 `go`）**

对照下一节就绪门禁。按确认卡上的模块范围组装任务包，**范围内按依赖连续做完**，不要每做完一个模块再要一次空 `go`。超出范围、挡开发、或要改已确认需求时停下再出卡。

## 设计契约（每个项目必做）

可独立成文（常见于 `documents/design/`）。最低包含：

1. **业务数据模型**：按需求规范 §4，草稿须人工确认后才能用于开发或 DDL。模型已存在（如中台表）仍须单独一份模型文档写清引用，经确认后再写契约。§2 为不用库 / 只用 mock 时：仍出这一份，只写实体、字段名、口径，禁止当 DDL。
2. **接口契约**：路径、入参出参语义、错误码；须能追溯到需求功能章节。需求正文禁止展开 API/DDL 全文。无库 / mock 时：写成对接约定（字段名、筛选、错误语义、`USE_MOCK`）；路径可写「本项目无独立 HTTP 资源」。
3. **横切约定**：登录与权限、全局时间/组织范围、环境与配置。不是单独功能章，也必须写明。无登录大屏可写「本项目无登录」等实际值，不得省略本节。

禁止在没有设计契约时临场编造接口或表结构并当作已确认需求。无库不是「可以不写契约」。

## 工作流程

```
PM 材料 + UI（用户只需提供；转规格由本 Skill 做）
  → 本份歧义清单（未确认先问），等用户回答
  → 本份确认卡（恰好一份文档、大屏 HTML 委托、或实现 Mx–My）
  → go 只写这一份 → 立刻独立机审（结论进 review/；HTML 走对方 5.5）→ 停，交人审
  → 用户手动确认下一步 → 下一份重复「问 → 卡 → go → 机审 → 人审」
     非大屏：需求规格 → 选型 §2 → 数据模型 → 设计契约 → 开发计划
     大屏：需求规格 → HTML → 选型 §2 → 数据模型 → 设计契约 → 开发计划
  →（实现 go：须计划已审核；卡上写 Mx–My 与完成口径；按 §2 写业务代码）范围内按依赖连续实现
  → 对照该口径自测通过后标「已完成」（不是必须真接口已测）
```

### 1. 写需求规格

读 [通用需求规格规范.md](通用需求规格规范.md)。落地步骤见 [通用需求规格规范-使用指南.md](通用需求规格规范-使用指南.md)；套模板见 [通用需求规格规范-示例.md](通用需求规格规范-示例.md)；字段表/流转/附录 F 草稿见 [通用需求规格规范-附录.md](通用需求规格规范-附录.md)。

要点：一章一个功能；确定 / 待定 / 示例必须分开；有界面必须 Markdown 链接原型（或「待生成」）；数据大屏类 HTML 原型委托 `bi-dashboard-generator`；涉及数据只引用模型，不在正文定义字段全集。

### 2. 确认技术栈

读 [通用技术选型规范.md](通用技术选型规范.md)。默认前端 Vue 3 + Element Plus，后端 Java + Spring Boot。不用默认值时在 §2 记覆盖，不要 silent 换栈。新项目沿用默认栈不逐项评审，但数据库等「按项目补充」项必须在确认卡和 §2 写出实际值（或「本项目不用」/「沿用本仓库：…」）。本规范不定义部署。

### 3. 数据模型与设计契约

模型草稿按需求规范 §4 与附录 F，人工确认后才能进契约或 DDL。无库 / mock 时仍出模型文档，按「无库压薄」写，禁止编 DDL。设计契约见上一节。

### 4. 拆开发计划

读 [通用开发计划管理规范.md](通用开发计划管理规范.md)。步骤见 [通用开发计划管理规范-使用指南.md](通用开发计划管理规范-使用指南.md)；拆分对照见 [通用开发计划管理规范-示例.md](通用开发计划管理规范-示例.md)。须设计契约已审核。须含基座/横切与联调模块。

依赖顺序：数据模型确认 → 接口/契约 → 前端 → 联调。按依赖推进，不按列表机械往下点。

### 5. 按模块执行

任务包见 [通用开发计划管理规范-附录.md](通用开发计划管理规范-附录.md)。禁止只丢模块名称。依赖未达到其所声明的完成口径（状态不是「已完成」）则标「阻塞」，不开下游。不要用「联调/真接口未做」挡住完成口径为「契约落盘」的前置模块。

对照本模块 **完成口径** 自测通过后，才能标「已完成」。编译通过不算完成。开始做改为「进行中」；依赖未齐改为「阻塞」。完成口径为「契约落盘」的模块，契约已写并经该口径核对即可完成，禁止因「没接后端 / 真接口未测」一直停在「进行中」。无库项目完成口径为「页面或 mock 自测」的模块，按该口径关闭。

范围内连续执行时：每做完一个模块更新状态，并写明刚完成什么；**下一步若仍在本次确认卡范围内，直接做，不要再要 `go`**。范围做完后，对该 Mx–My 做独立机审（结论进 `review/`），再建议下一张卡的口令。

## 写业务代码前的就绪门禁

收到写业务代码、按 §2 建前端/后端工程、或「转工程」的 `go` 时，未满足则停下、**不得出实现确认卡**：

- **「需求章节已确认」**：用户已看过**已落盘**的需求章节，或明确点名按该文件开发。只确认过聊天方案不算。「根据提供的材料开始开发」不算已确认规格。
- 选型 §2 已按确认卡填写（含数据库实际值或「本项目不用」）。
- 数据模型已确认；设计契约中与本模块相关的接口/横切已落盘并审核。
- **已审核的开发计划**已落盘；本模块在计划中，且依赖模块均为「已完成」（依赖所声明的完成口径，不是必须真接口已测）。
- 已按附录组装任务包；本模块**挡开发**待定已清或用户接受已知风险。
- 大屏：已审 HTML 已引用。非大屏：HTML 原型已引用或已写明不适用 / 待生成（待生成不挡写计划；挡的是转工程）。

**挡开发**（未清则停下提醒，不得写业务代码）：不选就无法实现，或一写就在多种算法/列集/空态里偷选。例如计算公司没写清、弹窗列未列、无数据怎么展示未写。

**不挡开工**（N.5 不要和上一类平铺成同等「待定」）：

- **已按正文默认**：材料只写了其中一种做法（如只写直连可点）→ 按已写的做，没写的不发明；若要改须另确认。
- **不列入本章待定**：已划出范围的不做项；仅联调才核对的映射笔误（开发先按材料原样）。

## 确定 / 待定 / 示例

| 类型 | 定义 | 标注 |
|---|---|---|
| 确定 | 用户已明确确认 | 正常表述 |
| 待定 | 未确认或多种方案 | `TODO` 或「待定：…」 |
| 示例 | 演示用占位 | 「示例 / 仅供参考 / 非最终方案」 |

禁止把待定/示例写成确定需求，禁止凭经验补用户未确认的细节。未询问的问题不得冒充「已按正文默认」。

## 合理化对照（写文档或代码时不准用这些借口）

| 借口 | 实际 |
|---|---|
| 「不挡拆章，可直接出确认卡」 | 会写成确定需求的冲突必须先问。 |
| 「建议写在确认卡上，一次 go 接受即可」 | 那是打包跳过询问。必须先问、等答。 |
| 「第一次 go 只写文档，三份都是文档」 | 不能一起出。一次一份，审完再下一步。 |
| 「用户说了根据提供的材料开始开发 / 梳理需求、制定计划、开发项目」 | 仍先只做需求规格：问 → 卡 → go → 审。开工话不是 `go`。 |
| 「轻量可以不写计划 / 实施顺序已经写在需求里 / 无库可以不写模型契约」 | 已取消分流。无库只压薄，仍各出一份。无已审核开发计划不得写业务代码。 |
| 「选型完了用户说下一步，默认按 Vue 写工程」 | 下一步按统一顺序，缺哪份补哪份；大屏缺 HTML 先委托 HTML；缺计划就出计划卡，不出实现卡。实现听 §2。 |
| 「材料短 / 我已经读懂了」 | 读懂不等于用户确认。未问不能成文。 |
| 「已经请用户手动审核了，不必再机审」 | 人审是授权门禁。必须先独立机审并落 `review/`。 |
| 「我刚写完，自己再过一遍清单就行」 | 写稿上下文不能当独立审。 |
| 「机审通过了，直接写下一份」 | 仍须用户确认下一步。 |

## 常见错误

- 没有 `go` 就改文件，或把「可以」当成授权
- 同一轮写出需求规格 + 选型 + 开发计划（或其中任意两份，含模型/契约）
- 一份落盘后不停、不出独立机审、只口头请用户审、接着写下一份
- 用机审通过代替用户「下一步」，或机审不经 `go` 改正文
- 未问清就把公式/范围/图字冲突写成确定；或用确认卡「接受建议」打包跳过询问
- 有挡住拆章/选型的歧义却未列清单、未等回答就出确认卡
- 未出确认卡，或同一轮 `go` 既写规格又写业务代码
- 把「根据提供的材料开始开发 / 转换并开发」当成一次写完文档和实现，或当成 `go`
- 把正文默认 / 范围外项 / 联调核对与挡开发项平铺成同等待定，或清单未清零就拒绝一切开工
- 无已审核开发计划、或无设计契约、或无已确认数据模型，就写业务代码 / 转工程
- 用「轻量 / 只有三章 / 需求里有实施顺序 / 无库」跳过模型、契约或计划
- §2 技术栈未填就把默认栈换成别的
- §2 数据库等实际值仍是「按项目补充」却声称选型已定或写业务代码
- 确认卡不写模块范围/完成口径，用空 `go` 连点每一个模块
- 用「没接后端 / 真接口未测」把完成口径为「契约落盘」或「页面或 mock 自测」的模块一直标「进行中」
- 模型草稿未确认就建表；无库项目把模型写成 DDL
- 无设计契约、无开发计划、或不组装任务包
- 只拆功能章，不做基座/联调
- 审核结论写进需求/设计正文；或该出 `review/` 却只聊天提醒
- 只拷 `SKILL.md`、不带同目录 `通用*.md` 附件
- 数据大屏类原型在本 Skill 内画图或写图表 option（应委托 `bi-dashboard-generator` 出 HTML）
- 大屏规格已审却跳过 HTML 直接选型或写工程
- 混合文档未先判定范围，或对方 Skill 未安装仍假装已委托
- 本 Skill 展开部署运维（按项目自己写 runbook）
