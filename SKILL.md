---
name: requirements-to-dev
description: Use when writing, reviewing, or revising 需求规格, 技术选型, 开发计划, or 设计契约; when starting a new business project from PM materials or UI; when the user mentions 功能章, 轻量路径, 完整路径, or 任务包.
---

# 需求到研发

从已有 PM 材料与 UI，产出需求规格，再按模块做到可验收。不覆盖编码风格、排期人力、部署运维、CI/Jenkins、本机 JDK 路径。

规模分流、就绪门禁、模块状态以**本 Skill**为准。协作门禁（`go` / 变更日志 / 审核外置 / bug 目录）：**项目已有总规则则用项目的**，本 Skill 不覆盖；项目没有总规则时，使用下一节默认门禁。不要求项目存在特定文件名或 Cursor `.mdc`。

**如何判定「已有总规则」**：项目规则目录里已有总规则正文（如 `AI工作规范.md` / 同名 `.mdc`，或该编辑器 always-apply 的等价规则）。不要凭 README 一句「请遵守 go」当成总规则。

细则按阶段再读**本目录**附件，不要把全部规范一次性塞进上下文。附件必须与 `SKILL.md` 放在同一目录，不要只拷主文件。

界面原型若是数据可视化大屏 / 看板 / 驾驶舱：委托 `bi-dashboard-generator` 出确认单与原型；本 Skill 只把原型路径写入需求章节，不在本 Skill 内画大屏或写图表 option。大屏确认之后若还要数据模型、接口契约或开发计划，仍由本 Skill 承接。

委托 / 交回时：先读对方 Skill 根目录的 `SKILL.md`（含该 `name` 与 `SKILL.md` 的目录）再动手。对方未安装则停，请用户安装或点名调用，禁止空口假装已按对方流程做完。

用户只说「按这份文档开始」、未点明只要大屏时，由本 Skill 先判定范围。文档同时含大屏页与中后台/CRUD 页：大屏页委托 `bi-dashboard-generator`，其余章节由本 Skill 写；禁止两边各做一整份。

## 协作门禁（仅当项目没有总规则时使用）

**没有 `go`，禁止任何文件的创建、修改、删除。**

- 一次 `go` = 已讨论确认的一份方案、一个开发模块、或已确认的一组文件。
- 用户说「好的 / 同意 / 可以」不算授权。
- 将改文件的方案讨论须：提出理解 → 列出问题 → 提出方案 → 等 `go`。纯问答（解释代码、查日志、问术语）直接答，仍不改文件。
- 读文件、搜索、分析、展示草稿：无需 `go`。
- 若环境支持子 Agent / 委派任务：主对话里已 `go` 确认的改文件任务，委派执行时免再次 `go`；不得扩大范围。无委派能力的环境忽略本条。
- 即使收到 `go`：有误解、缺关键信息、或未过就绪门禁时，停下再确认。

修改需求/设计文档后必须追加变更日志（时间 `YYYY-MM-DD HH:MM`、变更人、变更内容须引用章节名）。变更人未指定时写「用户确认（对话）」；禁止编造人名或伪造时间。

审核结论不写进正式文档正文，外置到被审文档所在目录的 `review/`（`X.md` → `review/X-审核-YYYYMMDD.md`）。

仅当用户明确说 bug / 缺陷 / 故障或要求记单时：先对齐描述；记录在 `./bugs/bug-YYYYMMDD-简述/`；解决前先写 `bug-fix-solution.md`。未点名 bug 的观感问题不强制建档。

## 规模分流（确认卡上必须写明；写计划或业务代码前不可跳过）

用户当场指定路径时以用户为准。未指定时，对下列条件计数，**满 2 条**走完整路径，否则**默认轻量**。仅「前后端技术分离」不计入。

计入的完整路径条件：功能章 ≥ 5；多端或多业务域；要分期交付；需要独立后端接口契约并做前后端联调。

| 路径 | 最终最低产物（分阶段写出，见下节） |
|---|---|
| **轻量路径** | 需求规格 + 界面原型（或写明不适用） |
| **完整路径** | 需求规格 + 界面原型 + 已确认数据模型 + 设计契约 + 开发计划 |

- 轻量路径**默认不写**独立开发计划；实施顺序写在需求文档中。用户当场点名「拆开发计划」时才写，且用本规范模块模板。
- 完整路径强制开发计划；须含基座/横切与联调模块，不能只拆功能章。
- 技术栈引用 [通用技术选型规范.md](通用技术选型规范.md) §2「本项目实际值」。未填写则先补覆盖表（实际值与默认不同须记覆盖原因），再写业务代码。不要在需求或开发计划里争论选型。

## 材料澄清（确认卡之前）

读完 PM 材料（含 Word/PDF/长文档）后，**先判断有没有挡住拆章或选型的不确定点**。有则只在对话里出歧义清单，**等用户回答后再出确认卡**。禁止未列清单就出确认卡，也禁止把未答的歧义写成确定需求。

歧义清单每条须含：问题、材料里的依据（章节/原句）、不问清会怎样（拆错章 / 选错栈 / 做成确定需求）。

**必须先问（答完才能出确认卡）**

- 功能边界对不上，无法列出功能章
- 大屏页与中后台页混在一起，范围未判定
- 角色、主成功场景互相矛盾
- 默认技术栈与材料明显冲突
- 扫描件/只有图没有字，读不出正文
- 无法判断轻量还是完整

**可进确认卡待定 / 规格 N.5，不必问完**

- 不挡本章范围的细节（文案、未写的增强、计分加权、以后再说的难度）
- 材料已写「待定 / 视情况 / 以后再说」且不影响拆章

材料短、且无上述必须先问的项：可直接出确认卡，待定写在卡上即可。

## 阶段确认（写任何落地文件前必做）

先在**对话里**出确认卡，**禁止先落文件**。须已做完上一节材料澄清（无必须先问的项，或用户已答）。用户对确认卡发 `go` 后，只写卡上列出的那一组文件。

确认卡必须包含：

1. **路径**：轻量 / 完整（写明计数依据；用户当场指定则写「用户指定」）。
2. **功能章清单**：每章名称 + 一句范围（含明确不做）。
3. **技术选型**：沿用默认栈，或列出覆盖项与原因。
4. **本次 `go` 只写哪些文件**：只能是「文档」或「实现」，不可两项同时写。

禁止：

- 未出确认卡，或卡上 1–4 未对齐，就写规格 / 选型 / 计划 / 代码。
- **同一轮 `go` 既写需求规格又写业务代码或可玩原型。**
- 把聊天里的口头方案当成「需求章节已确认」。
- 用户说「转换并开发 / 顺便做出来」时，仍先出确认卡；第一次 `go` **只写文档**。

例外：用户明确说「按已有的《…需求规格》开发」，且该章节文件已存在 → 确认卡可写「本次 go 写实现」，不再重写规格。

**文档阶段（第一次 `go`）**

| 路径 | 本阶段可写 |
|---|---|
| 轻量 | 需求规格 + 选型 §2；实施顺序写在需求里；不写独立开发计划（点名除外） |
| 完整 | 需求规格 + 选型 §2 + 设计契约 + 开发计划；模型待设计则起草草稿（仍待人工确认） |

文档阶段的界面原型：已有外部原型则只引用；尚未生成则标「待生成」。本阶段不新写可玩页。

**实现阶段（第二次及之后的 `go`）**

对照下一节就绪门禁。轻量按功能写原型或页面；完整按模块组装任务包，一次一个模块。

## 设计契约（完整路径必做）

可独立成文（常见于 `documents/design/`）。最低包含：

1. **业务数据模型**：按需求规范 §4，草稿须人工确认后才能用于开发或 DDL。
2. **接口契约**：路径、入参出参语义、错误码；须能追溯到需求功能章节。需求正文禁止展开 API/DDL 全文。
3. **横切约定**：登录与权限、全局时间/组织范围、环境与配置。不是单独功能章，也必须写明。

禁止完整路径在没有设计契约时临场编造接口或表结构并当作已确认需求。

## 工作流程

```
PM 材料 + UI
  →（有挡住拆章/选型的不确定点）对话歧义清单，等用户回答
  → 对话确认卡（路径 / 功能清单 / 选型 / 本次只写文档或实现）
  →（文档 go）需求规格 + 选型 §2
       → 模型待设计则起草并人工确认
       →（完整路径）设计契约 + 开发计划
  →（实现 go）按依赖逐模块组装任务包实现
  → 对照 N.4 验收后标「已完成」
```

### 1. 写需求规格

读 [通用需求规格规范.md](通用需求规格规范.md)。落地步骤见 [通用需求规格规范-使用指南.md](通用需求规格规范-使用指南.md)；套模板见 [通用需求规格规范-示例.md](通用需求规格规范-示例.md)；字段表/流转/附录 F 草稿见 [通用需求规格规范-附录.md](通用需求规格规范-附录.md)。

要点：一章一个功能；确定 / 待定 / 示例必须分开；有界面必须 Markdown 链接原型（或「待生成」）；数据大屏类原型委托 `bi-dashboard-generator`；涉及数据只引用模型，不在正文定义字段全集。

### 2. 确认技术栈

读 [通用技术选型规范.md](通用技术选型规范.md)。默认前端 Vue 3 + Element Plus，后端 Java + Spring Boot。不用默认值时在 §2 记覆盖，不要 silent 换栈。本规范不定义部署。

### 3. 拆开发计划（完整路径）

读 [通用开发计划管理规范.md](通用开发计划管理规范.md)。步骤见 [通用开发计划管理规范-使用指南.md](通用开发计划管理规范-使用指南.md)；拆分对照见 [通用开发计划管理规范-示例.md](通用开发计划管理规范-示例.md)。

依赖顺序：数据模型确认 → 接口/契约 → 前端 → 联调。按依赖推进，不按列表机械往下点。

### 4. 按模块执行

一次一个模块。任务包见 [通用开发计划管理规范-附录.md](通用开发计划管理规范-附录.md)。禁止只丢模块名称。依赖未完成则标「阻塞」，不开下游。

对照验收要点自测通过后，才能把模块标为「已完成」。编译通过不算完成。开始做改为「进行中」；依赖未齐改为「阻塞」。

每完成一个模块或轻量路径的一个功能，须写明：刚完成什么、下一步做什么、建议如何 `go`（例如 `go 执行 M1.3`）。

## 写业务代码前的就绪门禁

收到写业务代码或可玩原型的 `go` 时，未满足则停下：

- **「需求章节已确认」**：用户已看过**已落盘**的需求章节，或明确点名按该文件开发。只确认过聊天方案不算。
- **轻量路径**：本功能需求章节已确认；选型 §2 已按确认卡填写；原型已引用或已写明不适用 / 待生成；挡本功能的待定项已清或用户接受已知风险。
- **完整路径**：本模块需求章节 + 验收要点原文；已确认数据模型；设计契约中与本模块相关的接口/横切；开发计划中本模块依赖均为「已完成」；已按附录组装任务包。

## 确定 / 待定 / 示例

| 类型 | 定义 | 标注 |
|---|---|---|
| 确定 | 用户已明确确认 | 正常表述 |
| 待定 | 未确认或多种方案 | `TODO` 或「待定：…」 |
| 示例 | 演示用占位 | 「示例 / 仅供参考 / 非最终方案」 |

禁止把待定/示例写成确定需求，禁止凭经验补用户未确认的细节。

## 常见错误

- 没有 `go` 就改文件，或把「可以」当成授权
- 有挡住拆章/选型的歧义却未列清单、未等回答就出确认卡
- 未出确认卡，或同一轮 `go` 既写规格又写业务代码 / 可玩原型
- 把「转换并开发」当成一次写完文档和实现
- 未判定轻量/完整路径就开始写代码
- 轻量路径未点名「拆开发计划」却写完整开发计划
- §2 技术栈未填就把默认 Spring Boot 换成别的
- 模型草稿未确认就建表
- 完整路径无设计契约、无开发计划、或不组装任务包
- 只拆功能章，不做基座/联调
- 审核结论写进需求/设计正文
- 只拷 `SKILL.md`、不带同目录 `通用*.md` 附件
- 数据大屏类原型在本 Skill 内画图或写图表 option（应委托 `bi-dashboard-generator`）
- 混合文档未先判定范围，或对方 Skill 未安装仍假装已委托
- 本 Skill 展开部署运维（按项目自己写 runbook）
