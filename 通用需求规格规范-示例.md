# 通用需求规格说明书规范 - 完整示例

本文件配合《通用需求规格规范.md》使用，展示如何套用其模板。包含两类示例：列表查询 + 表单（CRUD），以及只读展示 / 报表。

> **关于「引用数据模型」的写法**：以下两个示例均假设**模型已存在**（如中台或既有系统提供），因此「引用数据模型」采用「文档文件名 + 模型章节 + 模型名称」的引用格式。如果你的项目属于**模型待设计**场景（本项目需根据需求设计数据库，常规管理系统的新建功能通常属于这种），「引用数据模型」应改写为业务实体名称并标注「模型待设计」，例如：
>
> - **引用数据模型**：客户（Customer）（模型待设计）
>
> 之后按《通用需求规格规范.md》§4 与附录 F 的流程，由 AI 起草模型草稿、开发/DBA 人工确认后产出正式模型文档，再补齐正式引用路径。

---

## 示例 A：列表查询 + 表单（CRUD）

```markdown
## 1. 客户信息管理

### 1.1 功能概述

- **功能类型**：列表查询 + 表单录入（新增/编辑）
- **用户角色**：客户运营专员、客户运营主管
- **用途**：维护客户基础资料，支持按条件查询与新增/编辑客户。
- **范围边界**：本功能仅维护客户主数据；不包含合同签订与应收账款（TODO：合同是否只读关联待定）。
- **主成功场景**：从菜单进入客户列表 → 查询并打开编辑 → 保存后列表展示最新资料。
- **适用端**：仅 PC

### 1.2 界面原型

[客户信息管理原型](./ui-design/客户信息管理.html)

页面主要区域：
- 查询条件区
- 客户列表区
- 新增/编辑入口

#### 界面交互

| 触发元素 | 触发方式 | 交互类型 | 目标 | 传递上下文 | 说明 |
|---|---|---|---|---|---|
| 新增按钮 | 点击 | 弹窗 | 客户编辑弹窗（见 1.3.3） | 无 | 空白表单 |
| 列表行-编辑 | 点击 | 弹窗 | 客户编辑弹窗（见 1.3.3） | 客户（customer_id） | 回填后保存 |

### 1.3 业务逻辑

#### 1.3.1 查询条件区

- **页面元素 / 操作点**：查询条件区（客户名称、客户状态、查询/重置）
- **引用数据模型**：《示例项目-需求规格-业务数据模型》3.3 客户（Customer）
- **功能要求**：支持按客户名称模糊查询、按客户状态精确筛选；点击查询刷新列表，点击重置恢复默认条件。
- **数据处理要求**：按客户名称（customer_name）模糊匹配、按客户状态（status）过滤。
- **权限与可见性**：具备「客户查询」权限的角色可见本区。
- **异常与空态**：不适用（条件区无独立空态）。

#### 1.3.2 客户列表

- **页面元素 / 操作点**：客户列表区（表格、分页）
- **引用数据模型**：《示例项目-需求规格-业务数据模型》3.3 客户（Customer）
- **功能要求**：展示客户名称、客户状态、更新时间；支持分页；默认按更新时间降序。
- **数据处理要求**：按查询条件过滤客户；按更新时间（updated_at）降序分页返回。
- **权限与可见性**：仅可查看当前用户数据权限范围内的客户。
- **异常与空态**：无数据时列表展示空态提示；无数据权限时提示无权限且不展示行数据。

#### 1.3.3 客户编辑弹窗

- **页面元素 / 操作点**：新增/编辑客户弹窗（表单与保存/取消）
- **界面原型**：[客户编辑弹窗原型](./ui-design/客户信息管理-客户编辑弹窗.html)
- **引用数据模型**：《示例项目-需求规格-业务数据模型》3.3 客户（Customer）
- **功能要求**：录入客户名称（必填）、客户状态（必填）；保存成功后关闭弹窗并刷新列表；取消关闭不保存。
- **数据处理要求**：新增时写入客户名称（customer_name）、客户状态（status）；编辑时按客户（customer_id）更新对应字段。
- **权限与可见性**：新增需「客户新增」权限；编辑需「客户编辑」权限。
- **异常与空态**：校验失败时在表单提示；保存失败时提示原因并保留已填内容。
- **前置条件 / 触发条件**：编辑时必须传入有效客户（customer_id）。

### 1.4 验收要点

1. 给定具备客户查询权限的用户，当按名称查询时，则列表仅展示匹配且在其数据权限内的客户。
2. 给定具备新增权限的用户，当保存合法表单时，则客户写入成功且列表可见新记录。
3. 给定无编辑权限的用户，当查看列表时，则不可见编辑入口或点击后提示无权限。
```

---

## 示例 B：只读展示 / 报表（简例）

```markdown
## 2. 销售月报查看

### 2.1 功能概述

- **功能类型**：只读展示 / 报表
- **用户角色**：销售管理者
- **用途**：按月查看销售额与订单量，辅助经营回顾。
- **范围边界**：只读查看与导出；不提供数据录入与审批。
- **主成功场景**：选择月份 → 查看汇总指标与明细表 → 可选导出。
- **适用端**：仅 PC
- **关键非功能约束**：支持导出当前查询结果为 Excel。

### 2.2 界面原型

[销售月报查看原型](./ui-design/销售月报查看.html)

页面主要区域：
- 月份筛选区
- 汇总指标区
- 明细表区

### 2.3 业务逻辑

#### 2.3.1 汇总指标区

- **页面元素 / 操作点**：汇总指标区（销售额、订单数）
- **引用数据模型**：《示例项目-需求规格-业务数据模型》3.1 销售订单（SalesOrder）
- **功能要求**：展示所选月份的销售额合计与订单数；切换月份后刷新。
- **数据处理要求**：按下单时间（order_time）落在所选月份过滤，对订单金额（amount）求和，并对订单计数。
- **权限与可见性**：仅销售管理者角色可进入；数据范围限定为其负责区域（TODO：区域规则待定）。
- **异常与空态**：所选月份无订单时指标展示为 0，并提示暂无明细。

#### 2.3.2 明细表与导出

- **页面元素 / 操作点**：明细表区及导出按钮
- **引用数据模型**：《示例项目-需求规格-业务数据模型》3.1 销售订单（SalesOrder）
- **功能要求**：展示订单明细列表；支持导出当前筛选结果。
- **数据处理要求**：与汇总同一过滤条件；导出字段与列表展示字段一致。
- **权限与可见性**：导出需具备「销售月报导出」权限。
- **异常与空态**：无明细时禁用导出或导出空模板（示例：仅供参考——空表头导出，最终方案待定）。

### 2.4 验收要点

1. 给定销售管理者，当选择某月时，则汇总指标与明细均对应该月数据。
2. 给定具备导出权限的用户，当点击导出时，则获得与当前筛选一致的 Excel。
```
