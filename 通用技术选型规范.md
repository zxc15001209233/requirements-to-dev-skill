---
alwaysApply: false
description: 项目技术选型的默认规范与覆盖规则。用于统一前后端语言/框架默认选择，供《通用开发计划管理规范》引用；当需要确认或变更项目技术栈时应用本规范。
---

# 通用技术选型规范

## 0. 使用说明

- 本文件定义**默认技术栈**，项目启动时可整体沿用，也可整体或部分覆盖；覆盖需在 §2「项目技术栈记录」表中记录并说明原因。写业务代码前须已填写 §2「本项目实际值」（见本 Skill《SKILL.md》规模分流）。
- 本规范**不定义**具体的编码规范、代码风格、详细目录结构、部署架构（属于工程规范/架构设计文档，外置）。
- 《通用开发计划管理规范》与需求规格文档中的技术栈信息，均**引用**本文件结论，不重复定义。

---

## 1. 默认技术栈

### 1.1 前端

| 层 | 默认选型 |
|---|---|
| 框架 | Vue 3 |
| UI 组件库 | Element Plus |
| 状态管理 | Pinia（历史项目可用 Vuex） |
| 路由 | Vue Router |
| HTTP 请求 | Axios |
| 构建工具 | Vite（历史项目可用 Vue CLI） |

### 1.2 后端

| 层 | 默认选型 |
|---|---|
| 语言 | Java |
| 框架 | Spring Boot（具体版本、单体/微服务架构由项目补充） |
| ORM | MyBatis / MyBatis-Plus（按项目补充） |
| 数据库 | 按项目补充（如 MySQL） |
| 缓存/中间件 | 按项目补充（如 Redis） |

---

## 2. 项目技术栈记录（每个项目落地时填写）

```markdown
| 层 | 默认值 | 本项目实际值 | 是否覆盖 | 覆盖原因 |
|---|---|---|---|---|
| {层名称} | {§1 默认值} | {实际值} | 是/否 | {原因，无覆盖则写「无」} |
```

**示例**（`sinopharm-dap-front`，历史项目）：

| 层 | 默认值 | 本项目实际值 | 是否覆盖 | 覆盖原因 |
|---|---|---|---|---|
| 前端框架 | Vue 3 | Vue 2 | 是 | 历史项目基于若依（RuoYi-Vue）框架延续，不做框架级重构 |
| UI 组件库 | Element Plus | Element UI | 是 | 同上 |

---

## 3. 覆盖规则

- **新项目**：无特殊原因，默认沿用 §1 默认技术栈，不需要逐项评审。
- **历史项目**：技术栈已固定，直接在 §2 表格中记录为「覆盖」，注明原因（如框架延续、历史遗留），**不因本规范发布而要求迁移**。
- **技术栈变更**（如新项目升级框架版本）：视为架构决策，需记录变更原因与影响范围，但本规范只记录选型结果，**不展开架构设计细节**。

---

## 4. 禁止项

- ❌ 在《通用开发计划管理规范》或需求规格文档中重新定义/争论技术选型（应引用本文件结论）。
- ❌ 历史项目因本规范发布被要求技术栈迁移（技术栈变更是独立的架构决策，非本规范目的）。
- ❌ 本文件展开具体的编码规范、代码风格、详细架构设计（属于工程规范/架构设计文档）。

---

## 5. 与其他文档的边界

| 内容 | 是否放在本规范 |
|---|---|
| 默认技术栈与项目覆盖记录 | ✅ |
| 详细架构设计（分层、部署拓扑） | ❌ 架构设计文档 |
| 编码规范/代码风格 | ❌ 工程规范文档 |
| 开发模块拆分与执行状态 | ❌ 《通用开发计划管理规范》 |
