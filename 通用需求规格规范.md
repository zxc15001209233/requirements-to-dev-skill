# 通用需求规格说明书规范

本文件为**核心规则**，目标是尽量精简、可快速扫描。完整示例见《通用需求规格规范-示例.md》；字段表/状态流转/导入导出/变更影响/业务实体草稿/反例等按需模板见《通用需求规格规范-附录.md》（与本文件同目录）。

---

## 0. 使用步骤与提交前自检

**使用步骤**：

1. 明确本次要写的**功能**边界（一个相对完整的业务能力 = 一章）。
2. 在「功能概述」标注**功能类型**，按第 3 节启用对应必选/可选小节。
3. 按模板填写：概述 → 界面原型（或「不适用」）→ 业务逻辑 →（可选）验收要点 →（可选）遗留问题。
4. 数据模型、接口契约、审核结论分别外置，不在正文展开定义或写入结论。
5. 提交前用下方清单自检。

**提交前自检清单**：

- [ ] 文档头含版本、适用范围；变更日志已更新（若有修改）
- [ ] 术语/缩略语已定义（若使用）
- [ ] 每章概述含功能类型、角色、用途、范围边界
- [ ] 确定 / 待定 / 示例已区分，无「待定当确定」
- [ ] 有 UI：有原型 Markdown 链接或「待生成」；无 UI：已写不适用及原因
- [ ] 非大屏有界面：N.2 已写「页面做成哪种」（A 或 B 原话，无设计稿则写「无设计稿，按 B」）；大屏章不写 A/B
- [ ] 交互在需要时已描述，且目标与业务逻辑名称一致
- [ ] 业务逻辑条目含功能要求；涉及数据则有模型引用与处理要求
- [ ] 权限与可见性、异常与空态已写或标「不适用」
- [ ] 建议具备可验证的验收要点
- [ ] 有 N.5 时已分挡开发 / 正文默认 / 不列入，待定表只含挡开发项
- [ ] 正文无数据模型定义全集、无审核结论、无 `<a href>`

---

## 1. 适用范围

- 本规范适用于**任意业务功能**的需求规格说明书，包括但不限于：列表查询、表单录入、详情、流程审批、只读展示/报表、批量导入导出、后台任务与接口能力等。
- 本规范**不绑定**某一类产品形态（如大屏、移动端、后台管理）；具体形态通过「功能类型」与「范围边界」声明。
- 一份文档可包含多个功能章节；也可按模块拆成多份文档，每份内部仍以功能为章节单位。

---

## 2. 文档结构

### 2.1 组织单位

- **以功能为组织单位**：一个相对完整的业务能力对应一个章节，依次编号 `## 1. {功能名称}`、`## 2. {功能名称}` ……
- 功能可由一个或多个页面/弹窗/任务承载；多页协同（如列表 → 详情 → 弹窗）优先写在**同一功能章**内，不必强行拆章。

### 2.2 文档级元信息（必选，宜短）

```markdown
# {文档标题}

## 文档说明
- **文档名称**：{名称}　**版本**：{如 v0.1}　**适用范围**：{系统/模块}

## 术语与缩略语（按需）
| 术语/缩略语 | 含义 |
|---|---|

## 变更日志
| 时间 | 变更人 | 变更内容（须引用章节名称） |
|---|---|---|
```

**要求**：修改后必须追加变更日志；禁止伪造历史时间/责任人、禁止用「后续补录」跳过；术语优先用业内规范用语，缩略语首次出现须定义。

### 2.3 确定 / 待定 / 示例（必选规则）

| 类型 | 定义 | 标注方式 |
|---|---|---|
| **确定内容** | 用户已明确确认的需求 | 正常表述 |
| **待定内容** | 尚未确认、存在多种方案 | `TODO` 或「待定：…」；可汇总到本章 3.5「遗留问题」 |
| **示例内容** | 仅用于演示布局/交互/文案的占位 | 明确标注「示例 / 仅供参考 / 非最终方案」 |

**禁止**：❌ 把待定/示例内容写成确定需求；❌ 基于经验补充用户未确认的细节并当作已定稿；❌ 过早锁定应保持灵活的交互或字段细节。

---

## 3. 功能章节模板

```markdown
## N. {功能名称}

### N.1 功能概述

### N.2 界面原型

#### 界面交互

### N.3 业务逻辑

### N.4 验收要点（可选）

### N.5 遗留问题（可选）
```

无界面的功能：`N.2` 写「不适用」并说明原因，不强制保留「界面交互」子章。

### 3.1 功能概述（必选）

**必须包含**（1–5 句）：

1. **功能类型**：列表查询 / 表单录入 / 详情只读 / 流程审批 / 只读展示或报表 / 批量导入导出 / 后台任务或接口能力 / 其他（需写明）
2. **用户角色**：谁使用
3. **用途**：解决什么问题、完成什么业务目标
4. **范围边界**：做什么；**明确不做什么**（暂无则写「待定」并标 TODO）

**可选**：主成功场景（一句话）、适用端（仅 PC / 含移动 / 嵌入其他系统）、关键非功能约束（点到为止，详细 NFR 外置）。

**禁止**：描述具体字段表、视觉样式、原型中不存在的能力（除非标为待定/示例）。

### 3.2 界面原型与交互

**有界面时**：

- **必须**用 Markdown 链接引用原型文件（待生成标注「待生成」），**禁止**手写 `<a href>`。
  ```markdown
  [{功能名称}原型](./ui-design/{功能名称}.html)
  ```
- **非大屏**必须在 N.2 写明「页面做成哪种」，用下面两句之一（对用户禁止写 EP、组件库等内部词）：
  - **A.** 按设计稿尽量长得一样
  - **B.** 布局跟说明或截图走，样子用系统自带的后台风格
  有完整设计图时默认建议 A；只有文档和大概截图时默认建议 B。有图则须在确认卡上问过并落盘，禁止静默挑选；材料完全没有设计图与界面截图时不必问，直接写「无设计稿，按 B」。实现跟这一句：A 尽量还原设计稿；B 用选型 §2 前端组件的默认样子。
- 可附简要区域说明；**禁止**描述字体/颜色/间距等原型已直观体现的视觉细节（档 A 的还原以设计稿为准，仍不要在需求正文里抄色值字号）。
- 数据可视化大屏 / 看板 / 驾驶舱：委托 `bi-dashboard-generator` 出确认单与原型，本规范只引用其路径。**不要**写上面的 A/B。
- 目录约定（可覆盖）：功能级放 `ui-design/` 根目录；跨功能复用元素放 `ui-design/_shared/`；单功能专属元素放 `ui-design/` 根目录并以「{功能名称}-」为前缀。

**无界面时**：`### N.2 界面原型` 下写「不适用。原因：{…}」。

**界面交互**（`#### 界面交互`，N.2 子章）：

- 原型已能直接体现该交互（点击即跳转/弹窗）时**可省略**；原型仅静态展示时**必须**描述。
- 每条须含：触发元素、触发方式、交互类型、目标（目标功能章节/弹窗名称）、传递上下文、必要的返回/关闭说明；弹窗名称须与业务逻辑条目一致。

  ```markdown
  | 触发元素 | 触发方式 | 交互类型 | 目标 | 传递上下文 | 说明 |
  |---|---|---|---|---|---|
  ```

### 3.3 业务逻辑（必选）

- 拆分为多个条目（`N.3.1`、`N.3.2` ……），每条对应**一个页面元素、操作点或处理步骤**（无 UI 按处理步骤拆分）。

**每条目标结构**（必选 + 按需）：

| 项 | 必选/按需 | 说明 |
|---|---|---|
| 页面元素 / 操作点 | 有 UI 必选；无 UI 改为「处理步骤」 | 对应原型区域、按钮或任务步骤 |
| 引用数据模型 | 涉及数据必选 | 模型已存在：文档文件名 + 模型章节 + 模型名称；模型待设计：写业务实体名称并标「模型待设计」；无数据写「不适用」 |
| 功能要求 | 必选 | 展示/录入/校验/按钮行为/状态等 |
| 数据处理要求 | 涉及数据必选 | 过滤、聚合、计算、持久化、关联等 |
| 权限与可见性 | 按需，无则「不适用」 | 角色、数据范围、按钮显隐、菜单/路由可进入性 |
| 异常与空态 | 按需，无则「不适用」 | 无数据、无权限、失败提示、冲突等 |
| 前置/触发条件 | 按需 | 按钮启用条件、流程进入条件、定时触发等 |
| 界面原型 | 可选 | 仅独立原型元素（弹窗/抽屉等）需要 |

**字段表述**：功能要求/展示类推荐**纯中文**；数据处理要求用**中文（英文字段名）**，如订单金额（amount）；**禁止**只用英文表名/字段名；**禁止**在展示/处理要求中重复赘述模型名称。

**无界面功能**：条目围绕触发方式、输入、处理规则、输出/落库/下游通知、失败策略（重试/告警/幂等）、权限展开，避免空写「页面元素」。

**按功能类型附加要求**（不适用则显式写明）：

| 功能类型 | 额外强调 |
|---|---|
| 列表查询 | 查询条件、结果列、分页排序、导出（见附录 A） |
| 表单录入 | 字段、校验、默认值、提交成功/失败结果（见附录 A） |
| 详情只读 | 展示字段、关联跳转（见附录 A） |
| 流程审批 | 状态、节点、角色、流转条件（见附录 B） |
| 只读展示/报表 | 展示形式、维度与度量、统计周期、刷新/筛选 |
| 批量导入导出 | 模板、校验、失败行处理、权限与导出范围（见附录 C） |
| 后台任务/接口能力 | 触发、入参出参语义、幂等、失败重试（详细 API 契约外置） |

### 3.4 验收要点（可选，建议默认生成简版）

每章末建议提供 3–8 条**可验证**条目，用「给定…当…则…」表述，避免空泛的「功能正常」：

```markdown
### N.4 验收要点
1. 给定{角色/条件}，当{操作}时，则{可验证结果}。
```

### 3.5 遗留问题（可选，存在待定内容时使用）

分三级写，**禁止**把三类平铺成同一张「待定」表：

1. **挡开发**：不选就无法实现，或一写就在多种算法/列集/空态里偷选。用下表；未清或用户未接受已知风险时，不得写本章业务代码。
2. **已按正文默认**：材料只写了一种做法。写明默认是什么；若要改须另确认。不挡开工。
3. **不列入本章待定**：已定不做的范围；仅联调才核对的源码/取值映射。可各用一句话指向概述或材料，不要占待定表。

表只给第 1 类。结论确定后转为正式描述并删除该行：

```markdown
### N.5 遗留问题

未清或未接受已知风险时，不得写本功能业务代码。下表只列挡住开工的项。

| 待定事项 | 影响范围 | 候选方案 | 需谁确认 |
|---|---|---|---|

**已按正文默认（不挡开工；若要改须另确认）**

- …

**不列入本章待定**

- …
```

---

## 4. 数据模型与外部文档引用

- 需求规格**禁止直接定义**数据模型（不得出现字段定义全集、约束、实体关系定义等）。
- 数据模型分两种场景，处理方式不同：

  | 场景 | 处理方式 |
  |---|---|
  | **模型已存在**（如中台/其他系统提供） | 直接**引用**独立的数据模型文档：文档文件名 + 模型章节 + 模型名称（可中英并列，如：销售订单（SalesOrder）） |
  | **模型待设计**（本项目需根据需求设计数据库） | 「引用数据模型」先写**业务实体名称**并标注「模型待设计」；由 AI 基于需求提取字段/关系**起草**模型草稿，经开发/DBA**人工确认**后产出正式模型文档，需求中再补齐正式引用路径 |

- 模型草稿**不得**未经确认直接执行为 DDL 或用于开发；确认前状态标「草稿」。
- 推荐模型文档命名 `《{项目}-需求规格-业务数据模型》`。
- 引用任何外部文档禁止写「见相关文档」，须可定位：`《{文档名}》{章节号} {名称}`。
- 接口设计、详细 API、DDL 属设计/模型文档，需求中只描述业务必需的输入输出语义。
- **严禁**在正文写入审核结论/评审结论/自检结论；审核记录外置到 `{需求文档目录}/review/`。
- 目录约定（项目可覆盖）：需求规格由项目约定；界面原型 `./ui-design/`；业务数据模型见独立模型文档；审核记录见上。

---

## 5. 跨功能关系（可选）

文档内存在多个功能或跨章跳转/复用时，可在相关功能的「界面交互」或概述边界中说明：入口来源、跳转目标（目标功能章节号/名称）、共享弹窗/复用元素（指向 `_shared` 或被复用章节）。

---

## 6. 禁止项汇总

- ❌ 在需求规格中直接定义完整数据模型（字段定义全集、约束、关系图等）。
- ❌ 数据处理要求只使用英文表名/字段名。
- ❌ 在展示/处理要求中重复赘述模型名称。
- ❌ 涉及数据的条目缺少「引用数据模型」（或未说明「不适用」）。
- ❌ 非大屏有界面却不写「页面做成哪种」（A/B），或对用户写成 EP / 组件库。
- ❌ 大屏章节误写后台 A/B。
- ❌ 描述原型中不存在的内容且未标为待定/示例。
- ❌ 把待定、示例写成确定需求。
- ❌ 在正文写入审核结论 / 评审结论 / 自检结论。
- ❌ 在界面交互中描述视觉样式动画，或出现业务逻辑未定义的跳转/弹窗目标。
- ❌ 无界面功能仍空写页面元素，却不写触发与处理步骤。
- ❌ 胡乱编造术语、缩略语且不加定义。

---

## 7. 参考资料

- **完整示例**（CRUD、只读展示/报表）：见《通用需求规格规范-示例.md》
- **按场景模板**（字段表、状态流转、导入导出清单、变更影响、业务实体草稿、反例）：见《通用需求规格规范-附录.md》

两份均与本文件同目录，非强制引用，按需查阅。

---

## 8. 与其他文档的边界

| 内容 | 是否放在本需求规格 |
|---|---|
| 功能行为、规则、权限、空态、验收 | ✅ |
| 界面原型链接与交互逻辑 | ✅ |
| 业务数据模型定义 | ❌ 外置模型文档 |
| API/DDL 全文 | ❌ 外置设计/模型文档 |
| 视觉色值字号动效 | ❌ 看原型/设计规范 |
| 排期与人力 | ❌ 项目计划 |
| 审核结论 | ❌ `review/` |
