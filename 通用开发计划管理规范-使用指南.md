# 通用开发计划管理规范 - 使用指南

本文件配合《通用开发计划管理规范.md》使用，说明如何从一份已定稿的需求规格文档，一步步拆出开发计划并执行。风格与《通用需求规格规范-使用指南.md》保持一致。

**路径**：先按本 Skill《SKILL.md》判定轻量路径 / 完整路径。开发计划是单独一份文档：须需求规格**已落盘且用户已审核并确认下一步**，选型 §2 已按串行顺序处理完毕（或用户点名本步就是写计划且选型已确认）。禁止与需求规格、选型同一轮 `go` 写出。本指南针对**完整路径**（强制写开发计划）。轻量路径默认不写独立开发计划；用户点名「拆开发计划」时才按本规范编写，仍单独一份。

---

## 整体流程

```
已确认需求文档 ─┐
                 ├─→ ① 拆分模块 → ② 排依赖 → ③ 关联技术栈 → ④ 执行 → ⑤ 验收自测与状态更新 → ⑥ 需求变更同步
```

---

## ① 拆分模块

- 前提：需求文档已完成《通用需求规格规范.md》§0 自检、评审，遗留问题已清零或已知悉可接受。
- 按需求文档的**功能章节**（`## N. {功能名称}`）拆分，一个功能章节对应一个或多个模块（`M{N}.{序号}`）。
- 拆分粒度参考技术分层：数据模型 / 接口 / 前端页面 / 前端组件 / 后台任务；简单功能可合并为一个模块，复杂功能按分层拆开（见《通用开发计划管理规范-示例.md》）。
- 每个模块的「引用需求章节」必须写具体章节号，不能写「见需求文档」。

---

## ② 排依赖

- 通用依赖顺序：**数据模型确认 → 接口/契约 → 前端页面/组件 → 联调**。
- 若需求中「引用数据模型」标注「模型待设计」，必须先有一个模型确认模块（引用《通用需求规格规范.md》§4 与附录 F 的起草+确认流程），其余依赖它的模块不能早于该模块按**完成口径**标「已完成」。
- 若「模型已存在」，可以跳过模型确认模块，直接从接口/前端开始排。
- 接口模块完成口径为「契约落盘」且已完成时，前端依赖已满足，不必等真接口联调。联调模块完成口径为「真接口联调」。

---

## ③ 关联技术栈

- 开发计划的「技术栈」字段直接引用《通用技术选型规范》§2 本项目实际值，不在开发计划里重新描述技术选型细节。
- 若本项目在技术选型规范中记录了「覆盖」（如历史项目沿用旧框架），开发计划按覆盖后的实际值执行，不需要额外说明。
- §2 数据库等实际值仍是「按项目补充」时，不得进入写业务代码的执行阶段。

---

## ④ 执行

- 按依赖顺序推进，模块状态从「待开始」→「进行中」→「已完成」（或「阻塞」）。「已完成」只表示本模块完成口径已过。
- 实现确认卡写明 Mx–My 后，范围内连续执行：每模块组装任务包（引用需求章节 + 验收对应 + 完成口径 + 技术栈），不要每模块再要一次空 `go`。
- 遇到依赖模块未达到其完成口径、需求不明确或技术问题时，状态标为「阻塞」并写明原因和需谁解决，不要保持「进行中」空转。不要用「真接口未测」把契约落盘模块标成阻塞。

---

## ⑤ 验收自测与状态更新

- 模块完成后，必须对照本模块「完成口径」实际自测（契约落盘 / 页面或 mock 自测 / 真接口联调），通过才标记「已完成」。验收对应（N.4）在该口径下走通即可。
- 不能仅凭代码写完、编译通过就标记「已完成」。完成口径不是真接口时，不要拿真接口未测当未完成。

---

## ⑥ 需求变更同步

| 变化 | 处理方式 |
|---|---|
| 需求文档变更日志有新记录 | 检查是否影响已拆分模块；受影响模块状态回退为「待开始」或「阻塞」，并在开发计划变更日志中记录 |
| 需求新增功能章节 | 补充对应的新模块 |
| 技术栈覆盖发生变化 | 更新开发计划「技术栈」字段，不需要逐模块重复说明 |

---

## 参考

- 拆分与模板规则：《通用开发计划管理规范.md》
- 完整示例（模型待设计 / 模型已存在两种场景对比）：《通用开发计划管理规范-示例.md》
- 需求来源：《通用需求规格规范.md》
- 技术栈来源：《通用技术选型规范.md》
