# 通用开发计划管理规范 - 附录

本文件配合《通用开发计划管理规范.md》使用，定义执行单个模块时需要组装的"任务包"，避免只把模块条目本身（指针）丢给大模型执行。

---

## 附录 A：模块执行任务包清单

执行任意模块前，必须组装以下内容；缺失项需先补齐或标注「无」再执行：

- [ ] 模块条目本身（模块名称、技术分层、依赖模块、验收对应）
- [ ] 依赖模块状态检查：依赖模块均已「已完成」，否则先处理依赖或将本模块改为「阻塞」
- [ ] 需求文档引用章节原文（功能要求、数据处理要求、权限与可见性、异常与空态、界面原型链接/交互）
- [ ] 验收要点原文（不是章节号，是具体条目内容）
- [ ] 技术栈实际值（《通用技术选型规范》§2 本项目实际值）
- [ ] 涉及数据的模块：数据模型原文（已确认模型文档对应章节字段；若仍是附录 F 草稿，需先确认状态已「已确认」）
- [ ] 界面原型文件（如有，引用 HTML 文件本身，而不仅是文字描述）
- [ ] （可选）项目现有代码上下文：相似功能的既有实现路径/组件，帮助保持风格一致

**禁止**：

- ❌ 只把开发计划模块条目（指针）丢给大模型，不提供需求原文、验收要点原文
- ❌ 依赖模块未完成时直接执行下游模块
- ❌ 数据模型仍是「草稿」未经人工确认，就据此生成代码或建表 DDL

---

## 附录 B：任务包组装模板

```markdown
## 执行任务包：M{N}.{序号} {模块名称}

### 模块信息
- 技术分层：{数据模型 / 接口 / 前端页面 / 前端组件 / 后台任务}
- 依赖模块状态：{均已完成 / 说明未完成项}
- 验收对应：{具体条目原文，非章节号}

### 需求原文（引用自《{需求文档}》{章节号}）
{粘贴该章节的功能要求、数据处理要求、权限与可见性、异常与空态}

### 界面原型
{Markdown 链接或截图，如有；无则写「不适用」}

### 技术栈
{引用《通用技术选型规范》§2 本项目实际值}

### 数据模型（涉及数据时必填）
{模型文档对应章节字段原文，或附录 F 已确认的草稿内容}
```

---

## 附录 C：AI 协作方式建议

- **在 Cursor / 支持文件读取的 Agent 环境中**：可直接让 AI 读取开发计划文档 + 需求文档 + 技术选型规范 + 模型文档，由 AI 自动组装任务包，无需手动摘录。
- **在无文件读取能力的纯 API/工具中**：必须先人工或用脚本按附录 B 模板组装好任务包文本，再作为 prompt 输入。
- 无论哪种方式，**执行完成后都要回到开发计划文档更新模块状态**，不能让 AI 生成代码后状态还停留在「待开始」。

---

## 附录 D：与核心规则的关系

| 内容 | 位置 |
|---|---|
| 模块拆分、依赖、状态定义 | 《通用开发计划管理规范.md》正文 |
| 任务包组装清单与模板 | 本附录 |
| AI 协作的整体流程 | 《通用开发计划管理规范-使用指南.md》④ 执行 |
